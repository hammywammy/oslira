[build]
  publish = "public"
  command = "node inject-env.js"
  edge_functions = "netlify/edge-functions"

[functions]
  directory = "netlify/edge-functions"

[[edge_functions]]
  path = "/api/*"
  function = "auth"

# Add environment variable injection for static files
[build.environment]
  NODE_VERSION = "18"
