[build]
  publish        = "public/pages"
  command        = "npm run build"
  edge_functions = "netlify/edge-functions"

[functions]
  directory = "netlify/edge-functions"

# Your existing auth edge function for all /api/* routes
[[edge_functions]] 
  function = "auth"
  path     = "/api/*"

